---
import Layout from "@/layouts/Layout.astro";
import SignUpForm from "@/components/auth/SignUpForm";

const error = Astro.url.searchParams.get("error");
---

<Layout title="Sign up | PrepToClimb">
  <div class="bg-cosmic flex min-h-screen items-center justify-center p-4">
    <div class="w-full max-w-sm rounded-2xl border border-white/10 bg-white/10 p-8 text-white backdrop-blur-xl">
      <h1
        class="mb-6 bg-gradient-to-r from-blue-200 to-purple-200 bg-clip-text text-center text-2xl font-bold text-transparent"
      >
        Create your PrepToClimb account
      </h1>
      <p class="mb-6 text-center text-sm leading-6 text-blue-100/70">
        Set up access now so you can move into the protected weekly-planning flow as soon as your account is ready.
      </p>
      <SignUpForm serverError={error} client:load />
      <p class="mt-4 text-center text-sm text-blue-100/60">
        Already registered? <a href="/auth/signin" class="text-purple-300 hover:underline">Sign in instead</a>
      </p>
    </div>
  </div>
</Layout>
