SUPABASE_URL=###
SUPABASE_KEY=###
# Required in production for account deletion (src/lib/supabase-admin.ts) and to run
# the e2e login test locally (e2e/global-setup.ts).
# Run `supabase start` then copy SERVICE_ROLE_KEY from `supabase status -o json`.
SUPABASE_SERVICE_ROLE_KEY=###